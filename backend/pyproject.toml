[tool.pytest.ini_options]
testpaths = ["tests"]
# So `import app` / `import scripts` resolve when running `pytest` from backend/.
pythonpath = ["."]
# Graders/examiner are async; let pytest-asyncio collect async tests without a marker.
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
